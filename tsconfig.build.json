{
  "extends": "./tsconfig.json",
  "exclude": ["node_modules", "test", "dist", "**/*spec.ts"]
  // added below to assist with path finding when it is moved
  "paths": {
    "@app/*": ["./src/*"]  // module-alias
  }
}
